package com.example.camera.model

/**
 * Enumeration of all central floating glass panels.
 * Only ONE panel can be active/visible at any given time.
 */
enum class CameraFloatingPanel {
    NONE,
    MASTER_CONTROLS,
    FILTER_TRAY,
    PORTRAIT_APERTURE,
    VIDEO_SETTINGS,
    MORE_MODES,
    QUICK_SETTINGS
}
